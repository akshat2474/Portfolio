name: portfolio_app
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  url_launcher: ^6.2.6
  font_awesome_flutter: ^10.7.0
  flutter_staggered_animations: ^1.1.1
  flutter_svg: ^2.0.10+1 
  any_link_preview: ^3.0.1
  flutter_html: ^3.0.0 
  flutter_html_iframe: ^3.0.0
  webview_flutter: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  # Declare the assets folder
  assets:
    - assets/images/
